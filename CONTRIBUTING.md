# 参与开发

1. 阅读 `ARCHITECTURE.md` 和 `docs/adr/`。
2. 新功能先写测试，尤其关注证据可见性、版本竞争、平台失败和降级。
3. 不提交平台 cookie、API key、`.env`、下载媒体、数据库转储和模型权重。
4. 文化材料示例必须有真实证据；自动化测试使用明确标记的合成夹具。
5. API schema 变更后运行 `make contracts`；数据库变更增加 Alembic migration，不修改已发布迁移。
6. 提交前运行 `make test`、`make check` 和浏览器测试。模型相关修改需要说明模型版本、硬件与实测样本。

欢迎优先贡献：中文梗检索评测、同名消歧、字幕定位、来源失效标记、模型适配器真实验收、可访问性和更细粒度的审核界面。
