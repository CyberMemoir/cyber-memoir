# V1 验证记录与边界

日期：2026-09-08。测试资料均为明确标记的合成夹具，不预装到公开档案。

## 已完成验证

| 检查 | 结果 |
|---|---|
| Python lint / format | 通过 |
| 后端自动化测试 | 56 项通过；真实服务测试在常规套件中单独跳过 |
| 独立真实服务集成测试 | 1 项通过：PostgreSQL、实际 pgvector 查询、OpenSearch BM25、Redis、S3 对象 |
| TypeScript / Next.js production build | 通过 |
| Playwright 浏览器验收 | 3 项通过：完整提交审核流程、手机导航/布局、无效审核令牌 |
| Alembic schema 对照 | 无未生成的迁移差异 |
| Docker 应用镜像 | Web / API / Worker / migration 构建成功，Compose 全栈运行 |
| PostgreSQL 备份恢复 | 已从 custom dump 恢复到独立空测试数据库，12 张表（含 Alembic）完整恢复 |
| 证据备份校验 | 导出、SHA-256 校验、拒绝覆盖非空存储、拒绝路径穿越的测试通过 |

后端覆盖：URL 去重、平台限制、内部地址阻断、审核权限、未公开证据不可见、完整定义引用、起源证据约束、版本冲突、幂等索引、撤回后旧索引失效、证据失效防护、合并历史、别名、多路检索降级、日期过滤、一跳关系、RAG 无答案、非法模型索引、MCP 只读工具、SRT/VTT/JSON 定位、平台失败、Redis 丢失轮询、租约恢复和限流。

真实服务测试使用独立 schema / index / bucket。向量值是测试替身，用来验证数据库向量通路，不属于 BGE 模型效果验证。

## 未冒充完成的外部验收

- 没有下载并运行 BGE-M3 / BGE Reranker / Whisper / PaddleOCR 全部模型权重；适配器已实现，真实权重、硬件和中文精度需要专门验收。
- 没有在真实平台账号会话下承诺任意 URL 都能获取；字幕/元数据路径已用平台格式夹具验证，失败可人工补料。
- 没有把合成测试结果当作真实梗检索 Recall、语义引用支持率或起源判断正确率。
- 没有配置公网域名、TLS、企业多用户身份、生产监控告警及异地备份。

## 浏览器与视觉验收

内置 Browser 连接返回无可用实例，发现列表为空，因此使用 Playwright Chromium。主屏参考由内置 Image Gen 生成，保存在 `docs/design/archive-concept.png`（1505 × 1045）。

使用 `view_image` 对参考图与浏览器截图逐一检查：

| 比较项 | 检查/修正 |
|---|---|
| 首屏文案 | 品牌、导航、双行标题、搜索 CTA、平台筛选及空态与参考一致；数量来自真实空库 |
| 布局 | 保留全宽检索、2.2:1 双栏、档案空态、原则侧栏与页脚；修正首屏纵向偏移 |
| 字体 | 宋体标题与系统无衬线界面，明确控件字号；生成图中文字栅格化差异不作为字体资源 |
| 色彩 | 白底、档案绿、浅绿侧栏和细边框，无新增渐变或纹理 |
| 图标 | 原生 SVG 的搜索、档案、信息与箭头；修正档案图标尺寸 |
| 响应式 | 390 × 844 手机视口检查导航、筛选、空态、表单，无横向溢出；修正提交页标题的孤立末行 |
| 交互 | 实际 API 驱动提交、材料、审核、检索、引用回答、详情与撤回，不使用截图/静态模拟替代功能 |

首屏文案差异：无新增营销文案；加载、错误和检索降级信息是必要运行状态。审核/提交/详情扩展使用同一视觉系统。临时 QA 图片和 trace 不进入仓库；长期保留设计参考和本记录。

## 复现

`make test`、`make check`、`make contracts`、`cd apps/web && npx playwright test`。

真实服务：`MEMOIR_INTEGRATION=1 uv run --env-file ../../.env pytest -q ../../tests/test_live_stack.py`（在 `apps/backend` 执行）。
